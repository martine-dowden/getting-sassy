{
  "name": "debugging-sass",
  "version": "1.0.0",
  "description": "When your Sass decides to be Sassy",
  "main": "index.js",
  "scripts": {
    "start": "npm run build:sass && http-server -a localhost -p 8080 -c-1 ./",
    "watch:sass": "sass --watch styles.scss styles.css",
    "build:sass": "sass styles.scss styles.css",
    "build": "npm run build:sass && gulp"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-clean-css": "^4.3.0",
    "gulp-htmlmin": "^5.0.1",
    "gulp-rename": "^2.0.0",
    "gulp-replace": "^1.1.3",
    "gulp-uglify": "^3.0.2",
    "http-server": "^14.1.0",
    "sass": "^1.54.8"
  }
}
